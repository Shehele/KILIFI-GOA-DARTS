// it executes a condition until its false
void main() {
  var a = 1;
  var maxnum = 10;
  while (a < maxnum) {
    print(a); //print untill its false
    a = a + 1; //increase a value of a after every iteartion/loop/cycle
  }
}
